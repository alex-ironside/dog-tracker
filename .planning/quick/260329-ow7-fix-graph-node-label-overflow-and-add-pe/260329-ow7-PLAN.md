---
phase: quick
plan: 260329-ow7
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/CompatibilityGraph.tsx
  - src/types/index.ts
  - src/components/WalkLogSheet.tsx
  - src/store/walkHistorySlice.ts
  - src/lib/scoring.ts
  - src/components/WalkHistory.tsx
  - src/components/WalkHistoryChart.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Graph node labels are readable — names render inside or cleanly centered below node circles without overflowing"
    - "When logging a walk, user can assign different outcomes per dog-pair (e.g., dog A incident with B, but dog C great with D)"
    - "Walk history entries display per-pair outcomes when present"
    - "inferStatusFromHistory uses per-pair outcomes when available, falls back to walk-level outcome for legacy entries"
  artifacts:
    - path: "src/components/CompatibilityGraph.tsx"
      provides: "Fixed nodeCanvasObject with background pill behind label text"
    - path: "src/types/index.ts"
      provides: "Updated WalkLogEntry with optional pairOutcomes field"
      contains: "pairOutcomes"
    - path: "src/components/WalkLogSheet.tsx"
      provides: "Per-dog-pair outcome UI in walk log form"
    - path: "src/lib/scoring.ts"
      provides: "Updated inferStatusFromHistory using pairOutcomes"
  key_links:
    - from: "src/components/WalkLogSheet.tsx"
      to: "src/types/index.ts"
      via: "WalkLogEntry.pairOutcomes"
      pattern: "pairOutcomes"
    - from: "src/lib/scoring.ts"
      to: "src/types/index.ts"
      via: "WalkLogEntry.pairOutcomes in inferStatusFromHistory"
      pattern: "pairOutcomes"
---

<objective>
Two fixes: (1) Graph node labels overflow outside circles — fix rendering so names are readable. (2) Walk log currently has a single outcome for all dogs — add per-dog-pair outcome mapping so dog A can have "incident" with B while C and D had "great" with each other.

Purpose: Graph readability fix and richer walk outcome data model.
Output: Readable graph labels + per-pair outcome logging UI and data model.
</objective>

<execution_context>
@.planning/quick/260329-ow7-fix-graph-node-label-overflow-and-add-pe/260329-ow7-PLAN.md
</execution_context>

<context>
@src/components/CompatibilityGraph.tsx
@src/components/WalkLogSheet.tsx
@src/types/index.ts
@src/lib/scoring.ts
@src/store/walkHistorySlice.ts
@src/components/WalkHistory.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix graph node label rendering</name>
  <files>src/components/CompatibilityGraph.tsx</files>
  <action>
Fix the `nodeCanvasObject` callback in CompatibilityGraph.tsx so labels are readable:

1. Replace the current `nodeCanvasObject` (lines 163-170) with a custom renderer that draws BOTH the node circle AND the label (change `nodeCanvasObjectMode` from `'after'` to `'replace'`).

2. The new nodeCanvasObject should:
   - Draw a filled circle (radius 6, matching `nodeRelSize={6}`) with fill `#3b82f6` (blue-500) and white 1.5px stroke
   - Measure the label text width using `ctx.measureText(label).width`
   - Draw a rounded-rect background pill (white fill, 0.7 alpha, 2px padding horizontal, 1px vertical) behind the text, centered below the circle at offset `y + 10/globalScale`
   - Draw the label text in `#1e293b` centered on the pill
   - Font size: `Math.max(10, 14 / globalScale)` px to keep labels legible at zoom-out but not huge at zoom-in
   - Set `nodePointerAreaPaint` to paint a transparent circle matching the node radius so click detection still works

3. Remove the current `nodeRelSize={6}` prop since we now draw the circle ourselves.

This ensures labels never spill outside — they get their own background pill below the dot.
  </action>
  <verify>
    <automated>cd "C:/Users/alex/Documents/private/dog tracker" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Graph node labels render with a background pill below the circle, fully contained and readable at any zoom level. Build passes with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add per-dog-pair outcome data model and WalkLogSheet UI</name>
  <files>src/types/index.ts, src/components/WalkLogSheet.tsx, src/store/walkHistorySlice.ts</files>
  <action>
**Types (src/types/index.ts):**
Add an optional `pairOutcomes` field to WalkLogEntry:
```typescript
export type WalkLogEntry = {
  id: string
  date: string
  outcome: WalkOutcome          // kept as walk-level default
  notes: string
  dogIds: string[]
  groupId?: string
  pairOutcomes?: Record<string, WalkOutcome>  // key = pairKey(idA, idB), value = outcome for that pair
}
```

**Store (src/store/walkHistorySlice.ts):**
No changes needed — `addWalkLog` already uses `Omit<WalkLogEntry, 'id'>` so pairOutcomes flows through automatically.

**WalkLogSheet UI (src/components/WalkLogSheet.tsx):**
After the current "Outcome" section (which becomes the walk-level default), add a conditional per-pair outcome section that appears when 2+ dogs are selected:

1. Add state: `const [pairOutcomes, setPairOutcomes] = useState<Record<string, WalkOutcome>>({})` — reset in the useEffect when sheet opens.

2. Import `pairKey` from `@/lib/scoring`.

3. Compute pairs from selectedDogIds using the combinatorial loop (i < j pattern). Only show this section when `selectedDogIds.length >= 2`.

4. Render a "Per-pair outcomes" section with a label "Per-pair outcomes (optional)" and helper text "Override the default outcome for specific pairs". For each pair, show:
   - A row with "{DogNameA} & {DogNameB}" label
   - A row of small outcome toggle buttons (same OUTCOME_OPTIONS, but use `size="sm"` and smaller text)
   - If no per-pair outcome is set, the row shows no selection (inherits walk-level default)
   - Clicking a button sets `pairOutcomes[pairKey(idA, idB)] = outcome`
   - Clicking the same button again removes that key (clears override, returns to default)

5. In `handleSave`, include pairOutcomes in the addWalkLog call only if the record has any entries:
   ```typescript
   const pairOutcomesPayload = Object.keys(pairOutcomes).length > 0 ? pairOutcomes : undefined
   useAppStore.getState().addWalkLog({
     date, outcome: outcome!, notes, dogIds: selectedDogIds,
     groupId: initialGroupId, pairOutcomes: pairOutcomesPayload,
   })
   ```

6. Remove walk-level outcome validation requirement when ALL pairs have explicit pairOutcomes set. The walk-level outcome is still required if any pair lacks an explicit override (it serves as the default). Update validation: if `selectedDogIds.length >= 2` and every pair has a pairOutcome entry, then walk-level outcome is optional (skip outcomeError). Otherwise keep current validation.
  </action>
  <verify>
    <automated>cd "C:/Users/alex/Documents/private/dog tracker" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>WalkLogEntry type has optional pairOutcomes field. WalkLogSheet shows per-pair outcome toggles when 2+ dogs selected. Users can override outcomes per pair. Build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Update inferStatusFromHistory and WalkHistory display for per-pair outcomes</name>
  <files>src/lib/scoring.ts, src/components/WalkHistory.tsx</files>
  <action>
**scoring.ts — inferStatusFromHistory:**
Update the function to check `pairOutcomes` first for per-pair data:

1. In the `pairWalks` filter loop, for each walk entry that contains both dogIdA and dogIdB:
   - First check `entry.pairOutcomes?.[pairKey(dogIdA, dogIdB)]` — if it exists, use that as the outcome for this pair on this walk
   - If no pairOutcome for this pair, fall back to `entry.outcome` (the walk-level default)

2. Collect the resolved outcomes into an array, then apply the existing priority logic:
   - Any 'incident' -> 'conflict'
   - Any 'poor' -> 'neutral'
   - >=50% good/great -> 'compatible'
   - else 'neutral'

This is backward-compatible: legacy entries without pairOutcomes use walk-level outcome as before.

**WalkHistory.tsx — WalkLogEntryRow:**
In the `WalkLogEntryRow` component, enhance the per-pair buttons to show the pair-specific outcome badge when `entry.pairOutcomes` exists:

1. Import `pairKey` (already imported).
2. For each pair button, check `entry.pairOutcomes?.[pairKey(idA, idB)]`.
3. If a pair-specific outcome exists, append a small colored dot or mini OutcomeBadge next to the pair name in the button to indicate its specific outcome differs from walk-level.
4. Format: `"{nameA} & {nameB} [Great]"` where `[Great]` is a tiny inline badge using the OUTCOME_BADGE colors, only shown if pairOutcome differs from `entry.outcome`.
  </action>
  <verify>
    <automated>cd "C:/Users/alex/Documents/private/dog tracker" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>inferStatusFromHistory correctly uses per-pair outcomes when available. WalkHistory pair buttons show per-pair outcome badges when they differ from walk-level default. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with no TypeScript errors
- Graph view shows dog names on readable background pills below node circles
- WalkLogSheet shows per-pair outcome section when 2+ dogs are selected
- Per-pair outcomes are optional — walk-level outcome serves as default
- inferStatusFromHistory uses pairOutcomes when present, falls back to walk-level outcome
- WalkHistory displays per-pair outcome indicators on pair buttons
</verification>

<success_criteria>
1. Graph node labels are fully readable at default zoom — no text overflow outside circles
2. Walk log form allows setting different outcomes per dog pair
3. Saved walk entries with pairOutcomes are correctly used by inferStatusFromHistory
4. Backward compatibility: existing walk entries without pairOutcomes work unchanged
</success_criteria>

<output>
After completion, create `.planning/quick/260329-ow7-fix-graph-node-label-overflow-and-add-pe/260329-ow7-SUMMARY.md`
</output>
