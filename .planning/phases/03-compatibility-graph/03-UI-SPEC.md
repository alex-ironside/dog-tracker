---
phase: 3
slug: compatibility-graph
status: draft
shadcn_initialized: true
preset: default / slate / cssVariables
created: 2026-03-27
---

# Phase 3 — UI Design Contract: Compatibility Graph

> Visual and interaction contract for Phase 3. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn (style: default) | components.json |
| Preset | default, base color: slate, CSS variables: true | components.json |
| Component library | Radix UI (via shadcn) | components.json |
| Icon library | lucide-react | DogPanel.tsx pattern |
| Font | System sans-serif (Tailwind default) | index.css — no custom font declared |

---

## Spacing Scale

Declared values (all multiples of 4). Pre-populated from existing App.tsx and DogPanel.tsx patterns.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, tight inline spacing |
| sm | 8px | Compact element spacing (e.g. status badge padding) |
| md | 16px | Default element spacing, form row gaps |
| lg | 24px | Section padding, sheet header/footer px |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions:
- Tab bar height: 44px minimum touch target per tab button (accessibility)
- Graph canvas: fills remaining viewport height after tab bar — no fixed px value

---

## Typography

Pre-populated from DogPanel.tsx and DogRoster.tsx observed patterns.

Weights reduced to 2 per contract rule: 400 (normal) and 600 (semibold). Label role merged from 500 into 600; Display role weight changed from 700 to 600.

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 14px (text-sm) | 400 (normal) | 1.5 | Status picker labels, node labels on graph canvas, node tooltips, sheet body text |
| Label | 14px (text-sm) | 600 (semibold) | normal | Form field labels, tab labels, status option labels |
| Heading | 18px (text-lg) | 600 (semibold) | 1.2 | Sheet title (dog names in edge-click sheet), panel headers |
| Display | 20px (text-xl) | 600 (semibold) | 1.2 | Page/section heading ("Compatibility") — reserved for section-level only |

Note: Node labels on the graph canvas use Body (14px / 400 / 1.5) rendered via ForceGraph2D `nodeCanvasObject`. This reuses the Body role — no additional size is needed.

---

## Color

Pre-populated from index.css CSS custom properties and observed Tailwind usage in existing components. Slate base color.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | hsl(0 0% 100%) — `--background` / `bg-white` | Page background, sheet backgrounds, graph canvas background |
| Secondary (30%) | hsl(210 40% 96.1%) — `--secondary` / `bg-slate-50` | Tab bar background, sheet header/footer, node fill |
| Accent (10%) | hsl(222.2 47.4% 11.2%) — `--primary` (dark navy) | Active tab indicator, primary CTA buttons ("Set compatibility"), focused ring |
| Destructive | hsl(0 84.2% 60.2%) — `--destructive` | "Remove" edge action in status picker sheet only |

Accent reserved for:
- Active tab underline/indicator
- Primary "Set compatibility" button (`variant="default"`)
- Focus ring on interactive inputs
- Selected status option highlight in edge-click sheet

Graph-specific semantic colors (NOT from the 60/30/10 palette — these are data colors for edges):

| Edge Status | Color | Tailwind Equivalent | CSS |
|-------------|-------|---------------------|-----|
| Compatible | Green | `text-green-500` | `#22c55e` |
| Neutral | Grey | `text-slate-400` | `#94a3b8` |
| Conflict | Red | `text-red-500` | `#ef4444` |
| Unknown | Grey dashed | `text-slate-300` stroke + SVG dashArray `5,5` | `#cbd5e1` dashed |

Node fill: `bg-slate-100` (#f1f5f9) with `text-slate-800` label. Active/hovered node: `bg-slate-200` (#e2e8f0).

---

## Component Inventory

All components to be used in this phase. No new primitives — all drawn from existing shadcn install.

| Component | Source | Usage in Phase 3 |
|-----------|--------|------------------|
| `Sheet` | `@/components/ui/sheet` (exists) | Edge-click status picker; node-click DogPanel wrapper |
| `SheetContent` | `@/components/ui/sheet` (exists) | Right-side drawer, `side="right"`, `w-full max-w-md` |
| `SheetTitle` | `@/components/ui/sheet` (exists) | Both dog names as sheet heading |
| `Button` | `@/components/ui/button` (exists) | Status picker options, Set compatibility/Discard changes actions, Remove |
| `AlertDialog` | `@/components/ui/alert-dialog` (exists) | NOT used in Phase 3 — no destructive confirmation needed beyond Remove edge |
| `ForceGraph2D` | `react-force-graph` (to be installed) | Main graph canvas component |
| `DogPanel` | `@/components/DogPanel` (exists) | Reused on node click — requires `open` + `onOpenChange` + `editingDog` props |

New component to build:

| Component | File | Description |
|-----------|------|-------------|
| `CompatibilityGraph` | `src/components/CompatibilityGraph.tsx` | ForceGraph2D wrapper; stable graphData via useMemo; edge/node click handlers |
| `EdgeSheet` | `src/components/EdgeSheet.tsx` | Sheet for edge-click: shows both dog names, 4 status buttons, Remove button |
| `CompatBadge` | `src/components/CompatBadge.tsx` | Coloured pill badge showing a CompatibilityStatus label (reused in Phase 4) |

---

## Layout Contract

### Tab Bar (new in App.tsx)

- Position: top of page, full width
- Height: 44px (touch target minimum)
- Tabs: "Dogs" | "Compatibility" — exactly two tabs
- Style: underline indicator on active tab using `--primary` color; inactive tabs use `text-slate-500`; tab bar background `bg-slate-50 border-b border-slate-200`
- Active tab: `font-semibold text-slate-900` + 2px bottom border in primary color
- Inactive tab: `font-medium text-slate-500 hover:text-slate-700`
- Implementation: local React `useState<'dogs' | 'compatibility'>` in App.tsx — no router

### Compatibility Tab Layout

```
┌─────────────────────────────────────────┐
│ [Dogs]  [Compatibility]                  │  ← 44px tab bar
├─────────────────────────────────────────┤
│                                         │
│         ForceGraph2D canvas             │  ← fills remaining height
│         (full width, flex-1)            │
│                                         │
└─────────────────────────────────────────┘
```

Graph canvas: `width: 100%, height: calc(100vh - 44px - tab-bar-height)` or `flex-1` inside a `flex flex-col min-h-screen` layout.

### Edge-Click Sheet (EdgeSheet)

Matches DogPanel sheet pattern exactly:

```
┌────────────────────────────────┐
│ [Dog A Name] & [Dog B Name]  X │  ← SheetTitle + close button, px-6 py-4 border-b
├────────────────────────────────┤
│ Current: [CompatBadge]         │  ← px-6 py-4
│                                │
│ Set compatibility:             │
│ [Compatible] [Neutral]         │  ← 4 status buttons, 2×2 grid
│ [Conflict]   [Unknown]         │
│                                │
│ ─────────────────────          │
│ [Remove]                       │  ← destructive, text-destructive
└────────────────────────────────┘
  sticky footer: [Discard changes] [Set compatibility] │  ← border-t, px-6 py-4
```

Status picker buttons: `variant="outline"` with a coloured left border matching edge color when selected. Selected state adds `ring-2 ring-offset-1` in the status color.

---

## Interaction Contract

### Graph Node

| Interaction | Behaviour |
|-------------|-----------|
| Hover | Node scales to 1.2× via ForceGraph2D `nodeCanvasObject` highlight; cursor `pointer` |
| Click | Opens DogPanel in edit mode (`editingDog` set to clicked dog, `panelOpen: true`) |
| Label | Dog name only, 14px (Body role), `text-slate-800`, centred below node circle |

### Graph Edge

| Interaction | Behaviour |
|-------------|-----------|
| Hover | Edge opacity increases from 0.6 to 1.0; cursor `pointer` |
| Click | Opens EdgeSheet with both dog names and current status |
| Render | ForceGraph2D `linkColor` + `linkWidth` + `linkLineDash` mapped from CompatibilityStatus |

### EdgeSheet Status Picker

| Interaction | Behaviour |
|-------------|-----------|
| Select status | Highlights that button (ring); does NOT save yet |
| Set compatibility | Calls `setCompatibility(idA, idB, selectedStatus)`; closes sheet |
| Discard changes | Discards selection; closes sheet |
| Remove | Calls `removeCompatibility(idA, idB)`; closes sheet; edge disappears from graph |

Remove does not require AlertDialog confirmation — the action is reversible (user can re-add by clicking the node pair and setting a new status). Remove button uses `text-destructive` color, `variant="ghost"`.

---

## Copywriting Contract

| Element | Copy | Source |
|---------|------|--------|
| Tab label: Dogs | "Dogs" | CONTEXT.md D-01 |
| Tab label: Compatibility | "Compatibility" | CONTEXT.md D-01 |
| Sheet title (edge click) | "{Dog A} & {Dog B}" | CONTEXT.md D-05 |
| Current status label | "Current status:" | default |
| Status picker heading | "Set compatibility:" | default |
| Status options | "Compatible" / "Neutral" / "Conflict" / "Unknown" | REQUIREMENTS.md COMPAT-01 |
| Primary CTA button | "Set compatibility" | checker revision — specific verb + noun, replaces generic "Save" |
| Cancel/dismiss button | "Discard changes" | checker revision — specific intent, replaces generic "Cancel" |
| Remove button | "Remove relationship" | default — verb + noun, unambiguous |
| Empty graph heading | "No compatibility data yet" | default |
| Empty graph body | "Add dogs in the Dogs tab, then click any two dogs to set their compatibility." | default |
| Error state | "Could not load graph. Try refreshing the page." | default |

No destructive AlertDialog confirmation for Remove — see Interaction Contract above.

---

## Accessibility Contract

- All Sheet dialogs include `SheetTitle` (Radix requirement — established in Phase 1 per DogPanel)
- Graph canvas (`<canvas>`) gets `aria-label="Dog compatibility graph"` and `role="img"` when no interactions are available
- Edge-click Sheet is keyboard accessible: Tab order is Discard changes → status buttons → Set compatibility → Remove
- Status buttons use `aria-pressed` to indicate selected state
- CompatBadge includes status text alongside colour (colour alone is insufficient per WCAG 1.4.1)
- Tab bar buttons use `aria-selected` and `role="tab"` / `role="tablist"` / `role="tabpanel"` pattern

---

## Graph Rendering Parameters

Prescriptive defaults for ForceGraph2D tuning (typical roster: 5–15 dogs):

| Parameter | Value | Reason |
|-----------|-------|--------|
| `nodeRelSize` | 6 | Readable node size at 5–15 nodes |
| `nodeLabel` | dog.name | Tooltip on hover |
| `linkWidth` | 2 | Compatible/Neutral; Conflict: 3 |
| `linkColor` | mapped from status (see Color section) | |
| `linkLineDash` | `[5, 5]` for Unknown; `[]` for others | CONTEXT.md D-04 |
| `d3VelocityDecay` | 0.4 | Faster settling, less thrash |
| `cooldownTicks` | 100 | Stop simulation after stabilising |
| `onEngineStop` | no-op or setState flag | Prevents constant re-render |
| `graphData` | via `useMemo` on dogs + compatEntries | CONTEXT.md — stable reference |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | sheet, button, input, label, alert-dialog, switch | not required |
| npm (react-force-graph) | ForceGraph2D | npm package, not a shadcn registry block — standard npm vetting applies; TypeScript types need verification before install (noted in STATE.md) |

No third-party shadcn registries declared. Registry safety gate: not applicable beyond official shadcn.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
